{
  "name": "auth-worker",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "build": "tsc && esbuild src/index.ts --bundle --platform=neutral --outfile=dist/worker.js",
    "deploy": "npm run build && wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "argon2": "^0.43.1",
    "hono": "^4.7.8",
    "nanoid": "^5.1.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250503.0",
    "@types/argon2": "^0.15.4",
    "typescript": "^5.8.3",
    "wrangler": "^4.14.1"
  }
}
